"use client";
import Link from "next/link";
import { FormEvent,useEffect,useState } from "react";
import { AppShell } from "../../components/app-shell";
import { supabase } from "../../lib/supabase";
export default function Account(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [signedIn,setSignedIn]=useState<string|undefined>();const [message,setMessage]=useState("");useEffect(()=>{supabase?.auth.getUser().then(({data})=>setSignedIn(data.user?.email));const {data}=supabase?.auth.onAuthStateChange((_e,s)=>setSignedIn(s?.user.email))??{data:{subscription:{unsubscribe(){}}}};return()=>data.subscription.unsubscribe();},[]);const submit=async(e:FormEvent,signup:boolean)=>{e.preventDefault();if(!supabase){setMessage("Account setup is not configured yet.");return;}const result=signup?await supabase.auth.signUp({email,password}):await supabase.auth.signInWithPassword({email,password});setMessage(result.error?.message??(signup?"Check your email to confirm your account.":"Signed in."));};if(signedIn)return <AppShell title="Account"><section className="empty"><h2>Signed in</h2><p>{signedIn}</p><p>Account identity is separate from your Athlete Profile.</p><Link href="/profile">Open Athlete Profile</Link><button className="quiet" onClick={async()=>{await supabase?.auth.signOut();setMessage("Signed out.")}}>Sign out</button><p className="status">{message}</p></section></AppShell>;return <AppShell title="Account"><form className="onboarding"><h2>Private account</h2><p>Your plans and optional health tracking are private to your account.</p><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={8} required/></label><button onClick={e=>submit(e,false)}>Sign in</button><button className="quiet" onClick={e=>submit(e,true)}>Create account</button><p className="status">{message}</p></form></AppShell>}
